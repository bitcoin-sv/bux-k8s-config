apiVersion: v1
kind: ConfigMap
metadata:
  name: bux-console-env
  labels:
    app: bux-console
data:
  env-config.json: |
    {
      "serverUrl": "https://bux.DOMAIN_NAME_TLD/v1",
      "hideServerUrl": false
    }
    
