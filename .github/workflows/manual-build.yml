name: Run bux-server docker image build manually
on:
  workflow_dispatch:
    inputs:
      revision:
        required: true
        type: string

jobs:
  build:
    uses: ./.github/workflows/build-bux.yml
    secrets: inherit
    with:
      version: ${{ inputs.revision }}
